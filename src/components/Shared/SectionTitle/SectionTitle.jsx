const SectionTitle = ({ title }) => {
  return (
    <div>
      <h2 className="text-[#050E6B] text-5xl font-bold text-center mb-14">
        {title}
      </h2>
    </div>
  );
};

export default SectionTitle;
